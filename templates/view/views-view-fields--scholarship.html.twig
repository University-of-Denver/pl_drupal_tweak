{#
/**
 * @file
 * Default view template to display all the fields in a row.
 *
 * Available variables:
 * - view: The view in use.
 * - fields: A list of fields, each one contains:
 *   - content: The output of the field.
 *   - raw: The raw data for the field, if it exists. This is NOT output safe.
 *   - class: The safe class ID to use.
 *   - handler: The Views field handler controlling this field.
 *   - inline: Whether or not the field should be inline.
 *   - wrapper_element: An HTML element for a wrapper.
 *   - wrapper_attributes: List of attributes for wrapper element.
 *   - separator: An optional separator that may appear before a field.
 *   - label: The field's label text.
 *   - label_element: An HTML element for a label wrapper.
 *   - label_attributes: List of attributes for label wrapper.
 *   - label_suffix: Colon after the label.
 *   - element_type: An HTML element for the field content.
 *   - element_attributes: List of attributes for HTML element for field content.
 *   - has_label_colon: A boolean indicating whether to display a colon after
 *     the label.
 *   - element_type: An HTML element for the field content.
 *   - element_attributes: List of attributes for HTML element for field content.
 * - row: The raw result from the query, with all data it fetched.
 *
 * @see template_preprocess_views_view_fields()
 *
 * @ingroup themeable
 */
#}
{% embed  "@molecules/scholarship-item.twig" %}
    {% set title = fields.title.content %}
    {% set description = fields.field_scholarship_description.content %}
    {% set kind = fields.field_scholarship_kind.content %}

    {% if 'Academic' in fields.field_scholarship_kind.content|render|render %}
      {% set card_kind_type = "Academic/Merit" %}
    {% elseif 'Financial' in fields.field_scholarship_kind.content|render|render %}
      {% set card_kind_type = "Financial Need" %}
    {% else %}
      {% set card_kind_type = '' %}
    {% endif %}

    {% if 'nofieldvalue' not in fields.field_scholarship_minimum_gpa.content|render|render %}
      {% set gpa = fields.field_scholarship_minimum_gpa.content %}
    {% else %}
      {% set gpa = '' %}
    {% endif %}

    {% if 'nofieldvalue' not in fields.field_scholarship_class_level.content|render|render %}
      {% set class_level = fields.field_scholarship_class_level.content %}
    {% else %}
      {% set class_level = '' %}
    {% endif %}

    {% if 'nofieldvalue' not in fields.field_scholarship_home_state.content|render|render %}
      {% set home_state = fields.field_scholarship_home_state.content %}
    {% else %}
      {% set home_state = '' %}
    {% endif %}

    {% if 'nofieldvalue' not in fields.field_scholarship_school.content|render|render %}
      {% set school = fields.field_scholarship_school.content %}
    {% else %}
      {% set school = '' %}
    {% endif %}

    {% if 'nofieldvalue' not in fields.field_scholarship_major.content|render|render %}
      {% set major = fields.field_scholarship_major.content %}
    {% else %}
      {% set major = '' %}
    {% endif %}

    {% if 'nofieldvalue' not in fields.field_scholarship_population.content|render|render %}
      {% set population = fields.field_scholarship_population.content %}
    {% else %}
      {% set population = '' %}
    {% endif %}

{% endembed %}
