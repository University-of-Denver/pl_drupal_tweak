{#
/**
 * @file
 * Display an iframe with alterable components.
 */
#}
<iframe title="{{ attributes.title }}" {% if url is not empty %} src="{{ url }}{% if query is not empty %}?{{ query | url_encode }}{% endif %}{% if fragment is not empty %}#{{ fragment }}{% endif %}"{% endif %}></iframe>
